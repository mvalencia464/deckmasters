{
  "name": "outdoor-living-contractor",
  "type": "module",
  "version": "0.1.0",
  "scripts": {
    "dev": "astro dev",
    "build": "node scripts/fetch-mgr-reviews.js && astro build",
    "preview": "astro preview",
    "deploy": "npm run build && npx wrangler pages deploy dist",
    "sync-reviews": "node --env-file=.env scripts/sync-reviews.js",
    "fetch-mgr-reviews": "node --env-file=.env scripts/fetch-mgr-reviews.js",
    "test:mgr-review": "node --env-file=.env scripts/test-mgr-review-payload.js"
  },
  "dependencies": {
    "astro": "^6.0.4",
    "tailwindcss": "^4.0.0"
  },
  "devDependencies": {
    "@astrojs/sitemap": "^3.7.2",
    "@tailwindcss/vite": "^4.0.0",
    "typescript": "^5.0.0"
  }
}
